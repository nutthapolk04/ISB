{
  "name": "isb-rfid-bridge",
  "version": "1.0.0",
  "description": "PC/SC RFID bridge for ACR1252U — reads card UID via pcsclite and broadcasts via WebSocket",
  "type": "module",
  "main": "rfid-server.ts",
  "scripts": {
    "start": "node rfid-server.js"
  },
  "dependencies": {
    "nfc-pcsc": "latest",
    "ws": "^8.14.2"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2"
  }
}
