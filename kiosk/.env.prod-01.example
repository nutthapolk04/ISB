# Device prod-01 — copy to .env.prod-01 (gitignored) before release build:
#   cp .env.prod-01.example .env.prod-01
#
# Credentials must match backend seed (backend-bun: bun run db:seed-kiosk):
#   KIOSK_SERVICE_USERNAME / KIOSK_SERVICE_PASSWORD

VITE_API_URL=https://your-backend.railway.app/api/v1
VITE_KIOSK_USERNAME=kiosk_service
VITE_KIOSK_PASSWORD=028510
VITE_KIOSK_APK_FILENAME=kiosk-prod-01.apk
