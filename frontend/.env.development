# Dev-only overrides — loaded by `vite dev`, NOT by `vite build` (production).
# EDC bridge (Paywire): point the SDK at the local mock bridge
# (docs/edc/sdk-js/mock-bridge → node server.js → 127.0.0.1:7331).
# Production builds fall back to the SDK default pos.local.bridge.schooney.tech,
# which the real paywire.exe bridge serves on the Windows POS.
# VITE_EDC_BRIDGE_DOMAIN=127.0.0.1
